/** Minimum touch-friendly height for registration flow controls (44px). */
export const REG_TOUCH_CLASS = "min-h-11";

/** Focus ring that does not shift layout (inset, no border color jump). */
export const REG_FIELD_FOCUS_CLASS =
  "focus-visible:border-input focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-ring/40";
